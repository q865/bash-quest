{
    "title": "Переменные и аргументы",
    "hint": "К аргументам скрипта можно обращаться через `$1`, `$2` и так далее.",
    "validation": [
        {
            "validator": "file_exists",
            "args": {
                "path": "greeting.sh"
            }
        },
        {
            "validator": "script_produces_output",
            "args": {
                "script": "./greeting.sh World",
                "output": "Hello, World"
            }
        }
    ]
}