{
    "title": "Простой веб-сервер",
    "task": "### Дедовский Ликбез\n\nNode.js родился, блядь, чтобы делать сервера. `express` — это минималистичный и охуенно популярный фреймворк, который делает эту задачу элементарной.\n\n**Установка:** `npm install express`\n\n**Пример:**\n```javascript\nconst express = require('express');\nconst app = express();\nconst port = 3000;\n\napp.get('/', (req, res) => {\n  res.send('Hello World!');\n});\n\napp.listen(port, () => {\n  console.log(`Сервер запущен на http://localhost:${port}`);\n});\n```\n\nЭтот код создаёт сервер, который на запрос `GET /` (главная страница) отвечает текстом `Hello World!`.\n\n### Твоя Задача\n\n**Напиши скрипт `server.js`, который запускает веб-сервер на порту 3000.**\n\nСервер должен:\n1.  На запрос `GET /` отвечать `"Welcome to the main page!"`.\n2.  На запрос `GET /ded` отвечать `"Ded is watching you."`.\n\nЯ проверю код, запускать его не буду.",
    "hint": "Создай два обработчика: `app.get('/', ...)` и `app.get('/ded', ...)`.",
    "cleanup": [
        {
            "action": "delete_file",
            "args": {
                "path": "server.js"
            }
        }
    ],
    "validation": [
        {
            "validator": "file_contains",
            "args": {
                "path": "server.js",
                "content": "express"
            }
        },
        {
            "validator": "file_contains",
            "args": {
                "path": "server.js",
                "content": "app.get('/ded'"
            }
        }
    ]
}