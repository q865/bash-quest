{
    "title": "Условия `if`",
    "hint": "Конструкция `if [ ... ]; then ... fi` позволяет выполнять команды в зависимости от условия.",
    "validation": [
        {
            "validator": "file_exists",
            "args": {
                "path": "check.sh"
            }
        },
        {
            "validator": "script_produces_output",
            "args": {
                "script": "./check.sh file.txt",
                "output": "exists"
            }
        }
    ]
}