{
    "title": "Свой веб-сервер",
    "cleanup": [
        {
            "action": "delete_file",
            "args": {
                "path": "server.js"
            }
        }
    ],
    "validation": [
        {
            "validator": "script_produces_output",
            "args": {
                "script": "node server.js & sleep 1 && curl -s http://localhost:1337 && kill $!",
                "output": "Zdarova, razebai!"
            }
        }
    ]
}
