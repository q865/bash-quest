{
    "title": "Скачивание файлов с `curl -O`",
    "cleanup": [
        {
            "action": "delete_file",
            "args": {
                "path": "robots.txt"
            }
        }
    ],
    "validation": [
        {
            "validator": "file_system_entry_exists",
            "args": {
                "path": "robots.txt"
            }
        }
    ]
}
