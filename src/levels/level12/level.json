{
    "title": "Заголовки в `curl`",
    "cleanup": [
        {
            "action": "delete_file",
            "args": {
                "path": "headers.txt"
            }
        }
    ],
    "validation": [
        {
            "validator": "file_system_entry_exists",
            "args": {
                "path": "headers.txt"
            }
        },
        {
            "validator": "file_content_matches",
            "args": {
                "path": "headers.txt",
                "content": "content-type: text/html"
            }
        }
    ],
    "hint": "Вспоминай команды для работы с сетью. Ответ не в твоей системе, а в интернете.",
    "hint2": "Используй `curl -I https://example.com > headers.txt`. Флаг `-I` (или `--head`) запрашивает только заголовки."
}
