{
    "title": "Планировщик `cron`",
    "hint": "Команда `crontab -e` открывает редактор для твоих `cron` задач.",
    "validation": [
        {
            "validator": "file_exists",
            "args": {
                "path": "cron_job.txt"
            }
        }
    ]
}
