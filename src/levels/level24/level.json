{
    "title": "Комбинация `curl` и `grep`",
    "validation": [
        {
            "validator": "command_succeeds",
            "args": {
                "command": "curl -s https://example.com | grep -q 'IANA'"
            }
        }
    ]
}
