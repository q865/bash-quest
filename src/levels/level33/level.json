{
    "title": "Функции в Bash",
    "hint": "Функции позволяют группировать команды и вызывать их по имени.",
    "validation": [
        {
            "validator": "script_produces_output",
            "args": {
                "script": "./my_func.sh",
                "output": "Hello from function"
            }
        }
    ]
}
